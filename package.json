{
  "name": "subzero",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist && echo 'Done.'",
    "start": "bun run build && bun dist/index.js",
    "dev:server": "bun src/index.ts",
    "build": "bun build src/index.ts src/miner.ts --outdir dist --target bun",
    "watch:server": "bun --watch run dev:server",
    "watch:client": "bun --watch run dev:client",
    "test": "bun test",
    "test:e2e": "LOG_LEVEL=info NODE_ENV=test bun test ./src/e2e/*.e2e.ts",
    "cov": "bun test --coverage"
  },
  "dependencies": {
    "@noble/ed25519": "^3.1.0",
    "@noble/hashes": "^2.2.0",
    "canonicalize": "^2.1.0",
    "ip": "^2.0.1",
    "level": "^10.0.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "webgpu": "^0.4.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ip": "^1.1.3",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
